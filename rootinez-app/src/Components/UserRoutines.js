const UserRoutines = () => {
    return ( 
        <div>
            <h1>Routines component to conditionally display if there is data for a user.</h1>
        </div>
    );
}
 
export default UserRoutines;